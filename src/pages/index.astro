---
const user = Astro.locals.user;

if (user?.role === 'student') return Astro.redirect('/student/dashboard');
if (user?.role === 'parent') return Astro.redirect('/parent/dashboard');

return Astro.redirect('/login');
---
